# Task 024-01 - Add App Context Docs

## Spec

`docs/specs/024-app-context-documentation.md`

## Plan

`docs/plans/024-app-context-documentation-plan.md`

## Goal

Create the canonical app context document for AI agents.

## Steps

1. Add `docs/app-context.md`.
2. Write the document in English.
3. Include app name, summary, target problem, core concepts, tabs, secondary views, storage behavior, constraints, and workflow references.
4. Keep the document focused on implemented behavior.

## Acceptance Criteria

- `docs/app-context.md` exists.
- The document is useful for onboarding another AI agent.
- The document describes all five current bottom navigation tabs.
- The document avoids planned/unimplemented behavior unless clearly labeled.

## Implementation Notes

- Added `docs/app-context.md` in English.
- Documented app name, summary, target problem, core concepts, primary tabs, secondary payment view, local data behavior, branding, stack, constraints, and agent workflow.
