# Task 046-02 - Add Period Total

## Objective

Restore the full period balance without removing the negative-only summary.

## Acceptance Criteria

- [x] `Total do período` adds every visible monthly balance.
- [x] `Total negativo no período` adds only visible balances below zero.
- [x] Both totals appear side by side with their labels and amounts aligned.
- [x] Both totals preserve negative signs when values are visible.
- [x] The eye toggle masks both complete values.
- [x] Focused tests pass.
- [x] README and app context are updated.
- [x] `npm run check` passes.
- [x] `npm run dup` passes.
