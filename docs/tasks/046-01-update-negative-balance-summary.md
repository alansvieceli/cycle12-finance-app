# Task 046-01 - Update Negative Balance Summary

## Objective

Implement `docs/specs/046-negative-balance-total.md` using
`docs/plans/046-negative-balance-total-plan.md`.

## Acceptance Criteria

- [x] `Pago vs Pendente — mês atual` is removed from `Gráficos`.
- [x] `Saldo por mês` still shows all configured visible months.
- [x] `Total negativo no período` adds only values below zero.
- [x] The visible total keeps its negative sign.
- [x] Hidden values use the existing full privacy mask.
- [x] README and app context are updated.
- [x] Focused tests pass.
- [x] `npm run check` passes.
- [x] `npm run dup` passes.
