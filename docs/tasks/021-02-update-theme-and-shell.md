# Task 021-02 - Update Theme and Shell

## Goal

Apply the Figma color tokens and replace the current horizontal tab shell with a fixed bottom navigation shell.

## Scope

- Update `src/theme/colors.ts`.
- Update shared button/tab styles as needed.
- Rework `src/FinanceApp.tsx` to use the new five-tab structure.

## Acceptance Criteria

- App uses the Figma palette.
- Bottom navigation has `Resumo`, `Gráficos`, `Planejar`, `Contas`, and `Ajustes`.
- Content remains scrollable above the fixed navigation.
