# Task 032-04 - Update Docs and Validate

Status: Completed

## Spec

`docs/specs/032-currency-input-mask.md`

## Plan

`docs/plans/032-currency-input-mask-plan.md`

## Goal

Document the masked currency input behavior and run the project validation commands.

## Steps

1. Update `docs/app-context.md` to describe the masked currency input behavior in the affected tabs and views.
2. Update `README.md` if it describes currency input behavior.
3. Run `npm run check`.
4. Run `npm test`.
5. Record the validation results in the plan file and mark plan/task statuses.

## Acceptance Criteria

- `docs/app-context.md` reflects the new input behavior.
- README is consistent with the app behavior.
- `npm run check` passes.
- `npm test` passes.
- Plan and task statuses are updated.
